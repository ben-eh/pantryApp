import { Typography, TextField, Button, Paper } from "@mui/material";
import { useState } from "react";
import styled from 'styled-components';

const CreateRecipe = () => {
    return (
      <Typography>CreateRecipe</Typography>
    );
}

export default CreateRecipe;