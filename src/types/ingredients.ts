export type Ingredient = {
	id: string,
	name: string,
	category: string
}

export type IngredientMap = {
	[id: string]: Ingredient
}

// export type Category = {
// 	id: string,
// 	name: string
// }

